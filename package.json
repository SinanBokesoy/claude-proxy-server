{
  "name": "claude-proxy-server",
  "version": "1.0.2",
  "description": "Railway server for claude-proxy-server token claiming - DEPLOY VERSION WITH ALL FIXES",
  "main": "server-deploy.js",
  "scripts": {
    "start": "node server-deploy.js",
    "dev": "node server-deploy.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.18.2",
    "googleapis": "^154.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
